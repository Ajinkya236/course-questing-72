import React from 'react';

// This component is no longer needed as actions are now in the media component
// Keeping as empty component to avoid breaking imports
const CourseActions: React.FC = () => {
  return null;
};

export default CourseActions;
